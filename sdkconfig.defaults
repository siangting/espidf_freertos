# 必要的 Arduino-as-component 設定
CONFIG_ARDUINO_AS_COMPONENT=y

# FreeRTOS tick rate 必須 1000
CONFIG_FREERTOS_HZ=1000

# 定義 kconfig 預設中用到卻沒定義的「no affinity」符號
# 這裡用 0xffffffff （32-bit 全 1），代表「不指定核心綁定」
CONFIG_FREERTOS_NO_AFFINITY=0xffffffff

# 以下五個 symbol 都以 no‐affinity 為預設
CONFIG_ESP_MAIN_TASK_AFFINITY=${CONFIG_FREERTOS_NO_AFFINITY}
CONFIG_ESP_TIMER_TASK_AFFINITY=${CONFIG_FREERTOS_NO_AFFINITY}
CONFIG_LWIP_TCPIP_TASK_AFFINITY=${CONFIG_FREERTOS_NO_AFFINITY}
CONFIG_FMB_PORT_TASK_AFFINITY=${CONFIG_FREERTOS_NO_AFFINITY}
CONFIG_MDNS_TASK_AFFINITY=${CONFIG_FREERTOS_NO_AFFINITY}
